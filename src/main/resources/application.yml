spring:
  profiles:
    active: local
    group:
      local:
        - secret
logging:
  level:
    com.study.delivery: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.orm.jdbc.extract: TRACE
    org.springframework.transaction.interceptor: TRACE
